## other

### calc在less中被编译的解决方法

在写普通的CSS样式时，计算属性的写法如下：

```css
.box {
  height: calc(100% - 50px);
}
```

在less中会把不需要编译时解析的算式意外解析，使浏览器拿不到需要的算式，以下是让less不要解析的语法:

```less
.box {
  height: calc(~"100% - 50px");
  // 效果相同
  height: ~"calc(100% - 50px)";
}
```